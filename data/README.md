# Data folder

This folder is reserved for example input data.

The empirical behavioral data used in the manuscript are not included in this public repository. To test the workflow, run:

```bash
python demo.py
```

The demo script generates a small synthetic input file at:

```text
data/example_input.csv
```

The synthetic example dataset is for testing the code workflow only and does not reproduce the manuscript results.
